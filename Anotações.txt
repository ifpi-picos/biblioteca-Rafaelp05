Classe:
É como um molde, um modelo. Define as propriedades (atributos) e comportamentos (métodos). Exemplo livro: atributos: título, autor. Métodos: emprestar(), devolver()

Objeto: É uma instância de uma classe, cada objeto tem sua cópia dos atributos definidos pela classe.